- name: loops demo
  hosts: local
  connection: local
  vars_prompt:
    name: "Name"
    prompt: "what is your name?"
    private: false

  tasks:
    - name: print names
      ansible.builtin.debug:
        msg: " HELLO {{ Name }}"
      loop:
       - chinnu
       - bob